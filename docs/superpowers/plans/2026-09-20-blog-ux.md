# Blog UX Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让博客的录入端不必手写 Markdown 语法、阅览端不再是一张白纸。

**Architecture:** 不动数据流（JSON store + Server Actions）与渲染管道（`react-markdown` + `remark-gfm`）。新增一层**纯逻辑 helper**（`src/lib/blog-content.ts`、`src/components/markdown-format.ts`）承载所有可测的规则，UI 组件只做渲染与调用；文章新增一个可选的 `category` 字段贯穿录入端与阅览端。

**Tech Stack:** Next.js App Router、React 19、Tailwind v4、`react-markdown` + `remark-gfm`、Node 内置测试运行器（`node:test`）。

**Spec:** `docs/superpowers/specs/2026-09-20-blog-ux-design.md`

## Global Constraints

- **保留 Markdown，不引入 WYSIWYG 或任何新的编辑器依赖**（无 Tiptap / Lexical / @tailwindcss/typography）。
- `BLOG_CATEGORIES` 固定为 4 个值：`Resume Tips` / `Design` / `Printables` / `Career`。分类**可选**，老文章没有该字段必须正常渲染。
- **分类筛选不得产生可索引的新 URL**（不能用 `?category=`）。用客户端状态。
- 所有新动效必须尊重 `prefers-reduced-motion`（项目已在 `ClientLayout` 用 `MotionConfig`、在 `globals.css` 用 media query 兜底，不要新增例外）。
- 深色底（hero）上的文字对比度必须 ≥ 4.5:1。
- 正文插图沿用现有上传端点：创作者 `/api/creator/upload`、管理员 `/admin/api/upload`，`type=image`，响应形如 `{ success: true, url }`。
- 纯逻辑测试用 `node --test`，文件与实现同目录、命名 `*.test.mjs`（参照 `src/db/blog-posts.test.mjs`）。
- UI 改动无法用 node:test 覆盖，按本项目既有做法验证：`next build` 后用 `curl` 抓渲染出的 HTML 断言**具体字符串**。

---

### Task 1: `category` 字段贯通数据层与所有构造点

`category` 是可选字段，TypeScript **不会**在漏传时报错 —— 而 `adminApprovePost` / `adminRejectPost` / `creatorCreatePost` / `creatorUpdatePost` 都是**逐字段重建对象**，漏一处分类就会在审核通过时被静默丢掉。

> **⚠️ 实施后更正的认知（Task 1 实测）**：把两个 input 类型的 `category` 声明为**必填但可为 undefined**，编译器报错的位置是**两个表单**（它们构造 input 对象），**不是**上面那 4 个构造点 —— 因为 `createPost`/`updatePost` 的入参是 `Omit<BlogPost, "id">`，其中 `category` 仍是可选的，漏传合法。
> **所以记录侧那 4 处 TypeScript 抓不住，只能靠本任务逐一列出。** 不要以为编译通过就万事大吉。

**Files:**
- Modify: `src/db/blog-posts.ts`
- Modify: `src/app/creator/dashboard/post-actions.ts:15-21`（`CreatorPostInput`）、`:40-63`（create）、`:65-96`（update）
- Modify: `src/app/admin/post-actions.ts:8-16`（`BlogPostInput`）、`:76-99`（approve）、`:101-118`（reject）
- Modify: `src/app/admin/AdminPostForm.tsx`、`src/app/creator/dashboard/CreatorPostForm.tsx`（仅为让 input 类型必填后编译通过，**Task 5 会重写这两行**）

**Interfaces:**
- Produces: `BLOG_CATEGORIES: readonly ["Resume Tips", "Design", "Printables", "Career"]`、`BlogPost.category?: string`
- Produces: `CreatorPostInput.category: string | undefined`、`BlogPostInput.category: string | undefined`

- [ ] **Step 1: 加字段与常量**

在 `src/db/blog-posts.ts` 的 `BlogPost` 接口里加一行（放在 `excerpt` 之后）：

```ts
  /** 展示用分类；取值来自 BLOG_CATEGORIES。老文章没有此字段。 */
  category?: string;
```

在同文件 `BlogStatus` 类型定义下方加：

```ts
/** 文章分类的固定取值 —— 列表页的筛选 tab 需要一个稳定的集合，所以不做自由输入。 */
export const BLOG_CATEGORIES = ["Resume Tips", "Design", "Printables", "Career"] as const;
```

- [ ] **Step 2: 把两个 input 类型改成必填**

`src/app/creator/dashboard/post-actions.ts` 的 `CreatorPostInput` 加：

```ts
  /** 必填但可为 undefined —— 漏传的调用点应当在编译期就暴露。 */
  category: string | undefined;
```

`src/app/admin/post-actions.ts` 的 `BlogPostInput` 加同样一行。

- [ ] **Step 3: 跑类型检查，看编译器指出了哪些点**

Run: `npx tsc --noEmit`
Expected: 报 `TS2345 Property 'category' is missing`，位置在 **`AdminPostForm.tsx` 与 `CreatorPostForm.tsx` 的 input 字面量**（不是 post-actions 的四个构造点 —— 见上方警告）。这两处随手补 `category: post?.category || undefined` 让树变绿即可；Task 5 会用新的 state 重写它们。

- [ ] **Step 4: 四处补上 category**

`creatorCreatePost` 的 `createPost({...})` 与 `creatorUpdatePost` 的 `updatePost(id, {...})` 各加一行：

```ts
      category: input.category,
```

`adminApprovePost` 与 `adminRejectPost` 的 `updatePost(id, {...})` 各加一行：

```ts
      category: existing.category,
```

（管理员侧的 `normalize()` 用的是 `...input` 展开，无需改。）

- [ ] **Step 5: 类型检查通过**

Run: `npx tsc --noEmit`
Expected: 无输出，exit 0。

- [ ] **Step 6: Commit**

```bash
git add src/db/blog-posts.ts src/app/creator/dashboard/post-actions.ts src/app/admin/post-actions.ts
git commit -m "feat(blog): add optional category field and carry it through every post write"
```

---

### Task 2: 内容规则纯逻辑 + 测试

把「摘要怎么来、读多久、相关文章/商品怎么挑、分类怎么筛」全部放进一个纯模块，这样它们是可测的，UI 只负责调用。

**Files:**
- Create: `src/lib/blog-content.ts`
- Test: `src/lib/blog-content.test.mjs`

**Interfaces:**
- Consumes: `BlogPost`（含 `category?`）来自 `@/db/blog-posts`（**仅类型导入**，见 Step 3 的说明）
- Produces:
  - `deriveExcerpt(content: string, max?: number): string`
  - `readingMinutes(content: string): number`
  - `filterByCategory<T extends { category?: string }>(posts: T[], category: string | null): T[]`
  - `selectRelatedPosts(posts: BlogPost[], currentId: string, n?: number): BlogPost[]`
  - `selectRelatedProducts(products: { id: string; category: string }[], postCategory: string | undefined, n?: number): typeof products`
  - `POST_CATEGORY_TO_PRODUCT: Record<string, string>`

> **⚠️ 这个模块必须零运行时依赖，这是实测结论，不是偏好。**
> `node --test` **解析不了 `@/` 别名**（探针实测：`ERR_MODULE_NOT_FOUND`），
> 而相对导入不带扩展名同样失败，带上 `.ts` 扩展名又会被 TypeScript 拒绝
> （`allowImportingTsExtensions` 是全局改动，不做）。
> 所以 `deriveExcerpt` 的截断逻辑**在本文件内内联**，不复用 `@/lib/seo` 的
> `metaDescription` —— 那 4 行重复是被测试运行器逼出来的，注释里要写明原因。

- [ ] **Step 1: 写失败的测试**

创建 `src/lib/blog-content.test.mjs`（测试文件用 `.mjs`，实现用 `.ts`，两者靠 `node --test` 的类型剥离衔接 —— 已实测可行）：

```js
import { test } from "node:test";
import assert from "node:assert";
import {
  deriveExcerpt,
  readingMinutes,
  filterByCategory,
  selectRelatedPosts,
  selectRelatedProducts,
} from "./blog-content.ts";
```

> 测试里**必须**写 `"./blog-content.ts"`（带扩展名）。不带扩展名会 `ERR_MODULE_NOT_FOUND` —— 实测确认。

```js
test("deriveExcerpt skips headings and returns the first prose paragraph", () => {
  const md = "## A heading\n\nFirst real paragraph of the post.\n\nSecond one.";
  assert.equal(deriveExcerpt(md), "First real paragraph of the post.");
});

test("deriveExcerpt strips inline markdown emphasis and links", () => {
  assert.equal(
    deriveExcerpt("Text with **bold** and a [link](https://x.test) inside."),
    "Text with bold and a link inside."
  );
});

test("deriveExcerpt truncates long prose at a word boundary, not mid-word", () => {
  // 钉死确切输出，不要只断言长度 —— `"word ".repeat(100)` 在第 39 字符处正好
  // 落在词尾，所以朴素的硬切实现也能满足「长度 ≤ 41 / 以省略号结尾」这类断言。
  assert.equal(deriveExcerpt("word ".repeat(100), 40), "word ".repeat(7).trimEnd() + "…");
  // 再来一个前缀截在词中间的输入，验证残缺词被丢弃而不是留下半截。
  assert.equal(deriveExcerpt("abcdefgh ".repeat(20), 40), "abcdefgh ".repeat(4).trimEnd() + "…");
});

test("readingMinutes is at least 1 and rounds to the nearest minute", () => {
  assert.equal(readingMinutes("short"), 1);
  assert.equal(readingMinutes("word ".repeat(220)), 1);
  assert.equal(readingMinutes("word ".repeat(330)), 2);
});

test("filterByCategory returns everything for null and matches exactly otherwise", () => {
  const posts = [{ category: "Design" }, { category: "Career" }, {}];
  assert.equal(filterByCategory(posts, null).length, 3);
  assert.equal(filterByCategory(posts, "Design").length, 1);
  assert.equal(filterByCategory(posts, "Nope").length, 0);
});

test("selectRelatedPosts prefers the same category and never returns the current post", () => {
  const mk = (id, category, publishedAt) => ({ id, category, publishedAt, title: id, slug: id, excerpt: "", content: "", status: "published", author: "x" });
  const posts = [
    mk("a", "Design", "2026-01-01"),
    mk("b", "Design", "2026-01-02"),
    mk("c", "Career", "2026-01-03"),
    mk("d", "Career", "2026-01-04"),
  ];
  const out = selectRelatedPosts(posts, "a", 3);
  assert.equal(out.length, 3);
  assert.ok(!out.some((p) => p.id === "a"));
  assert.equal(out[0].id, "b"); // same category first
});

test("selectRelatedPosts falls back to newest when the category is thin", () => {
  const mk = (id, category, publishedAt) => ({ id, category, publishedAt, title: id, slug: id, excerpt: "", content: "", status: "published", author: "x" });
  const posts = [mk("a", "Design", "2026-01-01"), mk("b", undefined, "2026-01-05"), mk("c", undefined, "2026-01-04")];
  const out = selectRelatedPosts(posts, "a", 2);
  assert.deepEqual(out.map((p) => p.id), ["b", "c"]);
});

test("selectRelatedProducts maps the post category to a product category", () => {
  const products = [
    { id: "1", category: "Templates" },
    { id: "2", category: "Photos" },
    { id: "3", category: "Templates" },
  ];
  const out = selectRelatedProducts(products, "Resume Tips", 2);
  assert.deepEqual(out.map((p) => p.id), ["1", "3"]);
});

test("selectRelatedProducts pads with other products when the mapped category is thin", () => {
  const products = [{ id: "1", category: "Photos" }, { id: "2", category: "Templates" }];
  const out = selectRelatedProducts(products, "Resume Tips", 3);
  assert.equal(out.length, 2);
});

test("selectRelatedProducts with no post category returns the first n products", () => {
  const products = [{ id: "1", category: "Photos" }, { id: "2", category: "Templates" }];
  assert.deepEqual(selectRelatedProducts(products, undefined, 2).map((p) => p.id), ["1", "2"]);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/lib/blog-content.test.mjs`
Expected: FAIL —— `Cannot find module .../blog-content.ts`（文件还没建）。

- [ ] **Step 3: 实现**

创建 `src/lib/blog-content.ts`：

```ts
/**
 * 该模块刻意零运行时依赖：`node --test` 无法解析 `@/` 路径别名，而相对导入
 * 必须带 `.ts` 扩展名（TypeScript 默认拒绝）。所以下面的截断逻辑是就地实现的，
 * 没有复用 `@/lib/seo` 的 `metaDescription` —— 别把它「优化」成 import。
 */

/** 文章分类 → 商品分类。没有映射的分类走「最新商品」兜底。 */
export const POST_CATEGORY_TO_PRODUCT: Record<string, string> = {
  "Resume Tips": "Templates",
  Career: "Templates",
  Design: "Photos",
  Printables: "Photos",
};

const WORDS_PER_MINUTE = 220;

/** 按词边界截断并补省略号 —— 与 `@/lib/seo` 的 metaDescription 同一策略。 */
function truncate(clean: string, max: number): string {
  if (clean.length <= max) return clean;
  const clipped = clean.slice(0, max - 1);
  const lastSpace = clipped.lastIndexOf(" ");
  return `${(lastSpace > max * 0.6 ? clipped.slice(0, lastSpace) : clipped).trimEnd()}…`;
}

/**
 * 摘要在卡片和 meta description 里都会用到，所以从正文首个**非标题、非引用、
 * 非代码块**段落提取 —— 直接截正文开头经常截到 "## What you get" 这种标题。
 */
export function deriveExcerpt(content: string, max = 160): string {
  const paragraphs = content.split(/\n{2,}/);
  for (const raw of paragraphs) {
    const block = raw.trim();
    if (!block) continue;
    if (/^(#{1,6}\s|>|\s*```|\s*[-*+]\s|\s*\d+\.\s|\|)/.test(block)) continue;
    const plain = block
      .replace(/!\[[^\]]*\]\([^)]*\)/g, "")    // images
      .replace(/\[([^\]]*)\]\([^)]*\)/g, "$1") // links keep their text
      .replace(/[*_`~]/g, "")
      .replace(/\s+/g, " ")
      .trim();
    if (plain) return truncate(plain, max);
  }
  return "";
}

/** 词数 ÷ 220，四舍五入到分钟，最少 1 分钟。 */
export function readingMinutes(content: string): number {
  const words = content.trim().split(/\s+/).filter(Boolean).length;
  return Math.max(1, Math.round(words / WORDS_PER_MINUTE));
}

/** `null` = 全部；否则精确匹配。没有分类的老文章只出现在「全部」里。 */
export function filterByCategory<T extends { category?: string }>(
  posts: T[],
  category: string | null
): T[] {
  if (!category) return posts;
  return posts.filter((p) => p.category === category);
}

/** 同分类优先，不足则按发布时间倒序补齐；永不返回当前篇。 */
export function selectRelatedPosts<
  T extends { id: string; category?: string; publishedAt?: string }
>(posts: T[], currentId: string, n = 3): T[] {
  const others = posts.filter((p) => p.id !== currentId);
  const current = posts.find((p) => p.id === currentId);
  const newest = (a: { publishedAt?: string }, b: { publishedAt?: string }) =>
    (b.publishedAt || "").localeCompare(a.publishedAt || "");

  if (!current?.category) return [...others].sort(newest).slice(0, n);

  const same = others.filter((p) => p.category === current.category).sort(newest);
  const rest = others.filter((p) => p.category !== current.category).sort(newest);
  return [...same, ...rest].slice(0, n);
}

/** 按映射挑同分类商品，不足则用其余商品补齐。 */
export function selectRelatedProducts<T extends { id: string; category: string }>(
  products: T[],
  postCategory: string | undefined,
  n = 3
): T[] {
  const target = postCategory ? POST_CATEGORY_TO_PRODUCT[postCategory] : undefined;
  if (!target) return products.slice(0, n);
  const matching = products.filter((p) => p.category === target);
  const rest = products.filter((p) => p.category !== target);
  return [...matching, ...rest].slice(0, n);
}
```

> `metaDescription` 已经做「按词边界截断 + 省略号」，所以 `deriveExcerpt` 不重复实现截断。
> `filterByCategory` 的测试用 `posts.length` 断言，所以返回类型用泛型即可。

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test src/lib/blog-content.test.mjs`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/lib/blog-content.ts src/lib/blog-content.test.mjs
git commit -m "feat(blog): pure helpers for excerpts, reading time and related content"
```

---

### Task 3: Markdown 工具栏的文本变换 + 测试

工具栏最容易出错的地方是**选区处理**（选中加粗、没选中插占位符、多行列表）。这部分是纯字符串逻辑，先测后写。

**Files:**
- Create: `src/components/markdown-format.ts`
- Test: `src/components/markdown-format.test.mjs`

> 这个模块**没有任何 import**，所以不受 Task 2 那个别名限制；测试里仍然要写带扩展名的 `"./markdown-format.ts"`。

**Interfaces:**
- Produces:
  - `type FormatAction = "bold" | "italic" | "h2" | "h3" | "ul" | "ol" | "quote" | "link" | "image"`
  - `applyFormat(value: string, start: number, end: number, action: FormatAction, url?: string): { value: string; selectionStart: number; selectionEnd: number }`

- [ ] **Step 1: 写失败的测试**

```js
import { test } from "node:test";
import assert from "node:assert";
import { applyFormat } from "./markdown-format.ts";

test("bold wraps the selection and keeps it selected", () => {
  const r = applyFormat("hello world", 0, 5, "bold");
  assert.equal(r.value, "**hello** world");
  assert.equal(r.value.slice(r.selectionStart, r.selectionEnd), "hello");
});

test("bold on an empty selection inserts a placeholder and selects it", () => {
  const r = applyFormat("abc", 3, 3, "bold");
  assert.equal(r.value, "abc**bold text**");
  assert.equal(r.value.slice(r.selectionStart, r.selectionEnd), "bold text");
});

test("italic uses a single asterisk", () => {
  assert.equal(applyFormat("hi", 0, 2, "italic").value, "*hi*");
});

test("heading prefixes the whole line the cursor sits on", () => {
  const r = applyFormat("line one\nline two", 12, 12, "h2");
  assert.equal(r.value, "line one\n## line two");
});

test("unordered list prefixes every selected line", () => {
  const r = applyFormat("a\nb", 0, 3, "ul");
  assert.equal(r.value, "- a\n- b");
});

test("ordered list numbers each selected line", () => {
  const r = applyFormat("a\nb", 0, 3, "ol");
  assert.equal(r.value, "1. a\n2. b");
});

test("quote prefixes every selected line", () => {
  assert.equal(applyFormat("a\nb", 0, 3, "quote").value, "> a\n> b");
});

test("link wraps the selection as link text", () => {
  const r = applyFormat("click here", 0, 10, "link", "https://x.test");
  assert.equal(r.value, "[click here](https://x.test)");
});

test("image inserts markdown image syntax with the uploaded url", () => {
  const r = applyFormat("abc", 3, 3, "image", "/uploads/x.png");
  assert.equal(r.value, "abc![](/uploads/x.png)");
});

test("bold on a reversed selection is handled the same as a normal one", () => {
  assert.equal(applyFormat("hello", 4, 1, "bold").value, "h**ell**o");
});

test("heading at the very start of a document that begins with a newline", () => {
  // start === 0 时不能走 lastIndexOf("\n", -1) —— 那会被钳到 0，把前缀加到第二行。
  const r = applyFormat("\nabc", 0, 0, "h2");
  assert.equal(r.value, "## \nabc");
  assert.equal(r.selectionStart, 0);
  assert.equal(r.selectionEnd, 3);
});

test("h3 uses three hashes", () => {
  assert.equal(applyFormat("abc", 1, 1, "h3").value, "### abc");
});

test("italic on an empty selection inserts and selects a placeholder", () => {
  const r = applyFormat("abc", 3, 3, "italic");
  assert.equal(r.value, "abc*italic text*");
  assert.equal(r.value.slice(r.selectionStart, r.selectionEnd), "italic text");
});

test("link with no url falls back to a bare https://", () => {
  const r = applyFormat("click", 0, 5, "link");
  assert.equal(r.value, "[click](https://)");
  assert.equal(r.value.slice(r.selectionStart, r.selectionEnd), "click");
});

test("image with a non-empty selection replaces it rather than wrapping", () => {
  assert.equal(applyFormat("abc", 1, 3, "image", "/x.png").value, "a![](/x.png)c");
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/components/markdown-format.test.mjs`
Expected: FAIL —— 函数未定义。

- [ ] **Step 3: 实现**

```ts
export type FormatAction =
  | "bold" | "italic" | "h2" | "h3" | "ul" | "ol" | "quote" | "link" | "image";

export interface FormatResult {
  value: string;
  selectionStart: number;
  selectionEnd: number;
}

/** 多行前缀类操作（列表/引用/标题）作用于光标或选区覆盖的每一行。 */
function mapLines(
  value: string,
  start: number,
  end: number,
  prefix: (index: number) => string
): FormatResult {
  // start === 0 必须单独处理：`lastIndexOf("\n", -1)` 按规范会被钳到 0，
  // 于是文档开头那个换行会被误当成「光标前的那一行」的分隔符，把前缀加到第二行上。
  const lineStart = start === 0 ? 0 : value.lastIndexOf("\n", start - 1) + 1;
  const lineEndIdx = value.indexOf("\n", end);
  const lineEnd = lineEndIdx === -1 ? value.length : lineEndIdx;
  const block = value.slice(lineStart, lineEnd);
  const next = block
    .split("\n")
    .map((line, i) => `${prefix(i)}${line}`)
    .join("\n");
  return {
    value: value.slice(0, lineStart) + next + value.slice(lineEnd),
    selectionStart: lineStart,
    selectionEnd: lineStart + next.length,
  };
}

export function applyFormat(
  value: string,
  start: number,
  end: number,
  action: FormatAction,
  url = ""
): FormatResult {
  // 反向选择（从右往左拖）在浏览器里会出现 start > end。
  const [from, to] = start > end ? [end, start] : [start, end];
  const selected = value.slice(from, to);

  const wrap = (token: string, placeholder: string): FormatResult => {
    const body = selected || placeholder;
    const next = `${value.slice(0, from)}${token}${body}${token}${value.slice(to)}`;
    const s = from + token.length;
    return { value: next, selectionStart: s, selectionEnd: s + body.length };
  };

  switch (action) {
    case "bold":
      return wrap("**", "bold text");
    case "italic":
      return wrap("*", "italic text");
    case "h2":
      return mapLines(value, from, to, () => "## ");
    case "h3":
      return mapLines(value, from, to, () => "### ");
    case "ul":
      return mapLines(value, from, to, () => "- ");
    case "ol":
      return mapLines(value, from, to, (i) => `${i + 1}. `);
    case "quote":
      return mapLines(value, from, to, () => "> ");
    case "link": {
      const body = selected || "link text";
      const next = `${value.slice(0, from)}[${body}](${url || "https://"})${value.slice(to)}`;
      const s = from + 1;
      return { value: next, selectionStart: s, selectionEnd: s + body.length };
    }
    case "image": {
      const next = `${value.slice(0, from)}![](${url})${value.slice(to)}`;
      return { value: next, selectionStart: from, selectionEnd: from };
    }
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test src/components/markdown-format.test.mjs`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/markdown-format.ts src/components/markdown-format.test.mjs
git commit -m "feat(blog): selection-aware markdown formatting actions"
```

---

### Task 4: `MarkdownEditor` 组件

**Files:**
- Create: `src/components/MarkdownEditor.tsx`

**Interfaces:**
- Consumes: `applyFormat`、`FormatAction`（Task 3）
- Produces: `<MarkdownEditor value={string} onChange={(v: string) => void} uploadUrl="/api/creator/upload" />`
  - `uploadUrl` 是上传端点路径（创作者与管理员的端点不同，由调用方传入）

> **`markdown.css` 目前只被 `src/app/blog/[slug]/page.tsx` 导入**，所以预览区拿不到
> 任何排版样式。本组件必须自己 `import "@/app/blog/markdown.css"` —— Next 会去重，
> 从两处导入同一个 CSS 文件是安全的。**不需要**为此改 markdown.css 本身
> （排版补齐是 Task 6 的事）。

- [ ] **Step 1: 实现组件**

要点（按 spec）：

```tsx
"use client";

import { useRef, useState } from "react";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import { applyFormat, type FormatAction } from "./markdown-format";
import { readingMinutes } from "@/lib/blog-content";
```

- `const ref = useRef<HTMLTextAreaElement>(null)` 持有 textarea。
- `run(action: FormatAction)`：读 `ref.current.selectionStart/End`，调 `applyFormat(...)`，`onChange(next.value)`，然后在 `requestAnimationFrame` 里把 `selectionStart/End` 写回并 `focus()`（必须先让 React 完成一次渲染，否则光标位置会被重置）。
- 工具栏按钮：B / I / H2 / H3 / • / 1. / " / 🔗 / 🖼。**每个按钮都要 `type="button"`**（表单内默认 submit）**和 `aria-label`**（图标按钮无可读文字）。
- 插图按钮：触发一个隐藏的 `<input type="file" accept="image/*">`，选中后 `FormData` POST 到 `uploadUrl`，拿 `data.url` 再 `run("image")`。
- 拖拽：编辑区监听 `onDrop`，`e.dataTransfer.files[0]` 走同一个上传函数。
- 下方 `min-h-[280px]` 的 textarea 与 `<div className="markdown-body">` 预览。
- 宽屏二栏（`lg:grid lg:grid-cols-2`），窄屏由 `const [previewOpen, setPreviewOpen] = useState(false)` 控制切换。切换按钮要有可访问的状态语义：`aria-pressed={previewOpen}` 且文字在「预览 / 编辑」间切换 —— 只靠样式变化，屏幕阅读器读不出当前处于哪一侧。
- 底部状态条：「约 N 分钟阅读」+「支持拖拽图片到正文里」。

- [ ] **Step 2: 构建验证**

Run: `npx tsc --noEmit && npx next build`
Expected: 无类型错误，构建成功。

- [ ] **Step 3: Commit**

```bash
git add src/components/MarkdownEditor.tsx
git commit -m "feat(blog): markdown editor with toolbar, live preview and inline uploads"
```

---

### Task 5: 两个表单接入编辑器与分类

**Files:**
- Modify: `src/app/creator/dashboard/CreatorPostForm.tsx`
- Modify: `src/app/admin/AdminPostForm.tsx`

**Interfaces:**
- Consumes: `MarkdownEditor`（Task 4）、`deriveExcerpt`（Task 2）、`BLOG_CATEGORIES`（Task 1）
- Produces: 两个表单的提交对象都带上 `category`

- [ ] **Step 1: 表单加状态**

两个文件各加：

```tsx
  const [category, setCategory] = useState(post?.category || "");
  const [excerptTouched, setExcerptTouched] = useState(!!post?.excerpt);
```

- [ ] **Step 2: 摘要自动生成**

把原来的 `onChange={(e) => setExcerpt(e.target.value)}` 换成：

```tsx
onChange={(e) => { setExcerpt(e.target.value); setExcerptTouched(true); }}
```

内容变更时联动（两个表单各自的 `content` 状态处）：

```tsx
const handleContentChange = (next: string) => {
  setContent(next);
  if (!excerptTouched) setExcerpt(deriveExcerpt(next));
};
```

摘要框下方加一行提示：`{!excerptTouched && <span>从正文自动生成 · 可直接修改</span>}`。

- [ ] **Step 3: 标题计数**

标题 input 旁加：

```tsx
<span className={title.length > 60 ? "text-amber-600" : "text-neutral-400"}>
  {title.length}/60
</span>
```

- [ ] **Step 4: 分类下拉**

加在 slug 之后：

```tsx
<label className="block text-xs font-medium text-neutral-700">{t("blog.field_category")}</label>
<select
  value={category}
  onChange={(e) => setCategory(e.target.value)}
  className="mt-1.5 block w-full rounded-lg border border-neutral-300 px-3.5 py-2.5 text-sm outline-none focus:border-blue-500"
>
  <option value="">—</option>
  {BLOG_CATEGORIES.map((c) => <option key={c} value={c}>{c}</option>)}
</select>
```

- [ ] **Step 5: 正文 textarea 换成 MarkdownEditor**

```tsx
<MarkdownEditor
  value={content}
  onChange={handleContentChange}
  uploadUrl={isCreatorForm ? "/api/creator/upload" : "/admin/api/upload"}
/>
```

- [ ] **Step 6: 提交对象带 category**

`CreatorPostForm` 的 `input` 加 `category: category || undefined`；`AdminPostForm` 同理。两个表单的 `blog.field_category` 文案加到 `LangContext` 的词表里（与既有 `blog.*` 条目放一起，中英各一条）。

- [ ] **Step 7: 构建验证**

Run: `npx tsc --noEmit && npx next build`
Expected: 无错误。

- [ ] **Step 8: Commit**

```bash
git add src/app/creator/dashboard/CreatorPostForm.tsx src/app/admin/AdminPostForm.tsx src/components/LangContext.tsx
git commit -m "feat(blog): use the shared editor in both post forms, add category and auto excerpt"
```

---

### Task 6: markdown.css 排版补齐

**Files:**
- Modify: `src/app/blog/markdown.css`

- [ ] **Step 1: 追加规则**

```css
.markdown-body h4 { font-size: 1rem; font-weight: 600; margin: 1.25rem 0 0.5rem; }
.markdown-body h5,
.markdown-body h6 { font-size: 0.9rem; font-weight: 600; margin: 1rem 0 0.5rem; color: #4b5563; }
.markdown-body img { border-radius: 0.75rem; margin: 1.25rem 0; }
.markdown-body figure { margin: 1.25rem 0; }
.markdown-body figcaption { margin-top: 0.5rem; font-size: 0.8125rem; color: #6b7280; text-align: center; }
.markdown-body hr { border: 0; border-top: 1px solid #e5e7eb; margin: 2rem 0; }
.markdown-body a:hover { color: #1d4ed8; }
.markdown-body h1 code, .markdown-body h2 code, .markdown-body h3 code { font-size: 0.9em; }
/* 表格在窄屏会撑破版式，所以让外层可横向滚动 */
.markdown-body .table-scroll { overflow-x: auto; margin: 0.75rem 0; }
```

- [ ] **Step 2: 表格外包滚动容器**

`src/app/blog/[slug]/page.tsx` 里给 `ReactMarkdown` 传自定义 `components`，让 table 渲染时套一层 `.table-scroll`：

```tsx
components={{
  table: ({ node, ...props }) => (
    <div className="table-scroll" tabIndex={0} role="region" aria-label="Table">
      <table {...props} />
    </div>
  ),
}}
```

`tabIndex={0}` 让键盘用户也能滚动 —— 否则横向滚动区对键盘不可达。

- [ ] **Step 3: 构建 + 视觉抽查**

Run: `npx next build && npx next start -p 39520`，然后：
`curl -s localhost:39520/blog/<某篇含表格的 slug> | grep -c "table-scroll"`
Expected: ≥ 1（该篇有表格时）。

- [ ] **Step 4: Commit**

```bash
git add src/app/blog/markdown.css "src/app/blog/[slug]/page.tsx"
git commit -m "style(blog): complete markdown typography and make tables scrollable"
```

---

### Task 7: 详情页 —— hero、进度条、文末模块、JSON-LD

**Files:**
- Create: `src/components/ReadingProgress.tsx`
- Create: `src/components/PostEndMatter.tsx`
- Modify: `src/app/blog/[slug]/page.tsx`

**Interfaces:**
- Consumes: `readingMinutes`、`selectRelatedPosts`、`selectRelatedProducts`（Task 2）；`getAllProducts`（`@/db/products-store`）；`getCreators`（`@/db/creators`）；`NewsletterSection`（现有组件）
- Produces: `<ReadingProgress />`、`<PostEndMatter post={post} relatedPosts={...} relatedProducts={...} authorUsername={...} />`

> **⚠️ 本任务与 Task 6 改同一个文件 `src/app/blog/[slug]/page.tsx`。**
> Task 6 会给 `<ReactMarkdown>` 加上 `components={{ table: ... }}`（表格横向滚动）。
> 本任务重写 hero 区块时**必须保留那个 prop** —— 只替换 h1 + 封面那一段，
> 不要整段重写 `<article>`。
>
> 另外：**相关文章要用 3 列网格**（`grid gap-4 sm:grid-cols-3`）而不是横排。
> Task 8 会把 `BlogCard` 从「小横卡」改成「竖版大图卡」，网格布局在改造前后都成立，
> 横排则会在 Task 8 之后被挤变形。

- [ ] **Step 1: `ReadingProgress`**

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

export default function ReadingProgress() {
  const [pct, setPct] = useState(0);
  const frame = useRef(0);

  useEffect(() => {
    const onScroll = () => {
      // rAF 节流：scroll 事件每秒可能触发上百次。
      if (frame.current) return;
      frame.current = requestAnimationFrame(() => {
        frame.current = 0;
        const max = document.documentElement.scrollHeight - window.innerHeight;
        setPct(max > 0 ? Math.min(100, (window.scrollY / max) * 100) : 0);
      });
    };
    onScroll();
    window.addEventListener("scroll", onScroll, { passive: true });
    return () => {
      window.removeEventListener("scroll", onScroll);
      if (frame.current) cancelAnimationFrame(frame.current);
    };
  }, []);

  return (
    <div className="fixed inset-x-0 top-0 z-[200] h-[3px] bg-transparent" aria-hidden="true">
      <div className="h-full bg-blue-600 transition-[width] duration-150" style={{ width: `${pct}%` }} />
    </div>
  );
}
```

`aria-hidden` 因为它是纯装饰；`transition-[width]` 已由 `globals.css` 的 `prefers-reduced-motion` 规则统一降为 0.01ms。

- [ ] **Step 2: `PostEndMatter`**

三块 + 订阅，全部 server-render：

```tsx
export default function PostEndMatter({ post, authorUsername, relatedPosts, relatedProducts }) {
  return (
    <section className="mt-14 border-t border-neutral-200 bg-neutral-50 px-6 py-12">
      <div className="mx-auto max-w-3xl">
        {/* 作者卡 */}
        {/* 相关文章：relatedPosts.map -> <BlogCard /> */}
        {/* 相关商品：绿色系卡片，链接到 /products/[id] */}
        {/* 订阅：<NewsletterSection /> */}
      </div>
    </section>
  );
}
```

- 作者卡用 `post.author` + `authorUsername`；没有 `authorUsername` 时不渲染「View Store」按钮。
- 相关文章放在 `grid gap-4 sm:grid-cols-3` 里（见上面的说明）。
- 相关商品卡片必须用**绿色系边框**（`border-emerald-300` / `bg-emerald-50`）与文章卡片区分 —— spec 明确要求避免广告感。
- 每块各自判空：数据为空就整块不渲染，不出现空标题。

- [ ] **Step 3: 详情页接入**

`src/app/blog/[slug]/page.tsx`：

```tsx
const relatedPosts = selectRelatedPosts(await getPublishedPosts(), post.id, 3);
const products = await getAllProducts().catch(() => []);
const relatedProducts = selectRelatedProducts(
  products.map((p) => ({ id: p.id, category: p.category, name: p.name, price: p.price, previewImages: p.previewImages })),
  post.category,
  3
);
```

hero 区块（替换现有的 h1 + 封面）：

```tsx
<div className="relative h-[320px] w-full overflow-hidden sm:h-[420px]">
  {post.coverImage ? (
    <Image src={post.coverImage} alt="" fill priority sizes="100vw" className="object-cover" />
  ) : (
    <div className="h-full w-full bg-neutral-900" />
  )}
  <div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent" />
  <div className="absolute inset-x-0 bottom-0 mx-auto max-w-3xl px-6 pb-8">
    {post.category && (
      <span className="mb-3 inline-block rounded-full bg-blue-100 px-2.5 py-1 text-[11px] font-bold uppercase tracking-wide text-blue-800">
        {post.category}
      </span>
    )}
    <h1 className="text-3xl font-extrabold tracking-tight text-white sm:text-4xl">{post.title}</h1>
    <div className="mt-3 flex items-center gap-3 text-sm text-white/80">
      {/* 作者 / 日期 / 约 N 分钟阅读 */}
    </div>
  </div>
</div>
```

`priority` 是必须的 —— hero 图是 LCP 元素。`fill` 需要父容器 `relative`（已满足）。

- [ ] **Step 4: 补 `BlogPosting` JSON-LD**

在 `<article>` 内加一段 `<script type="application/ld+json">`，字段：`@type: "BlogPosting"`、`headline: post.title`、`image`（绝对 URL，无封面则用 `/og-default.png`）、`datePublished: post.publishedAt`、`author: { "@type": "Person", name: post.author }`、`articleSection: post.category`。**`articleSection` 只在有分类时输出** —— 输出空字符串会被判为无效标注。

- [ ] **Step 5: 构建 + HTML 断言**

Run: `npx next build && npx next start -p 39520`
```bash
curl -s localhost:39520/blog/<slug> | grep -c "BlogPosting"      # 期望 ≥1
curl -s localhost:39520/blog/<slug> | grep -c "KEEP READING"     # 期望 ≥1
curl -s localhost:39520/blog/<slug> | grep -c "SHOP THE LOOK"    # 期望 ≥1
```
无封面的降级与无分类的降级各用一篇对应文章验证，期望仍是 200 且无空标签。

- [ ] **Step 6: Commit**

```bash
git add src/components/ReadingProgress.tsx src/components/PostEndMatter.tsx "src/app/blog/[slug]/page.tsx"
git commit -m "feat(blog): hero layout, reading progress and end-of-article modules"
```

---

### Task 8: 列表页 —— 置顶大卡、竖版网格、分类筛选

**Files:**
- Modify: `src/components/BlogCard.tsx`
- Create: `src/components/PostList.tsx`（client，承载筛选状态）
- Modify: `src/app/blog/page.tsx`

**Interfaces:**
- Consumes: `filterByCategory`（Task 2）、`BLOG_CATEGORIES`（Task 1）
- Produces: `<PostList posts={...} />` —— 客户端组件，接收全部已发布文章

- [ ] **Step 1: `BlogCard` 改竖版大图**

现在是 64×96 的小横卡。改成：顶部 16:9 封面（`next/image` + `sizes`），下方标题、分类标签、日期。保持 `href={`/blog/${post.slug}`}` 不变。

- [ ] **Step 2: `PostList` 客户端组件**

```tsx
"use client";
import { useState } from "react";
import { filterByCategory } from "@/lib/blog-content";
import { BLOG_CATEGORIES } from "@/db/blog-posts";
```

- 状态：`const [category, setCategory] = useState<string | null>(null)`
- tab 集合：`["All", ...BLOG_CATEGORIES.filter(c => posts.some(p => p.category === c))]` —— **只显示真正有文章的分类**，空的不要出现。
- 置顶大卡：筛选结果的第一篇，横向布局（左图右文 + 摘要）。**筛选后同样生效**，不是永远显示最新那篇。
- 其余：`grid gap-6 sm:grid-cols-2 lg:grid-cols-3` 的竖版卡片。
- 空结果：显示「这个分类下还没有文章」+ 一个「查看全部」按钮，不要留白。
- **不改变 URL** —— 纯 `useState`，不写 `?category=`。

- [ ] **Step 3: 页面接入**

`src/app/blog/page.tsx` 保持 server component，把 `posts` 传给 `<PostList posts={posts} />`。

- [ ] **Step 4: 构建 + HTML 断言**

Run: `npx next build && npx next start -p 39520`
```bash
curl -s localhost:39520/blog | grep -c "All"                 # tab 渲染
curl -s localhost:39520/blog | grep -c 'href="/blog/'        # 置顶 + 网格都有链接
curl -s localhost:39520/blog | grep -c "category="           # 必须是 0 —— 没有可索引筛选 URL
```

- [ ] **Step 5: Commit**

```bash
git add src/components/BlogCard.tsx src/components/PostList.tsx src/app/blog/page.tsx
git commit -m "feat(blog): featured card, vertical grid and client-side category filter"
```

---

### Task 9: 内容回填与封面重出

数据在服务器上（本地没有 `data/blog-posts.json`），所以这一步是**服务器端一次性脚本**，不进代码库。

**Files:**
- Create（临时，跑完删除）: `/opt/kyno/scripts/backfill-blog-category.mjs`
- Use: `scripts/make-blog-covers.js`

- [ ] **Step 1: 先备份**

```bash
ssh kyno 'cd /opt/kyno && cp data/blog-posts.json data/blog-posts.json.bak-category-$(date +%Y%m%d%H%M%S)'
```

- [ ] **Step 2: 回填分类**

脚本按 slug 精确匹配写死映射（照抄即可）：

```js
const MAP = {
  "resume-writing-checklist-for-2026-50-checks-before-you-hit-submit": "Resume Tips",
  "cover-letter-checklist-for-2026-40-checks-before-you-hit-submit": "Resume Tips",
  "job-interview-checklist-for-2026-50-things-to-do-before-during-and-after": "Career",
  "new-grad-job-search-field-guide": "Career",
  "how-to-display-kids-art": "Printables",
  "home-office-wall-ideas": "Design",
  "family-photo-wall-without-custom-framing": "Design",
};
```

脚本要：读 JSON → 只给 `category` 为空的文章赋值 → **打印每篇的 slug 与新分类** → 写回。跑完核对输出，确认 7 篇全部落到预期分类，没有落到 `undefined`。

- [ ] **Step 3: 重出方图封面**

`scripts/make-blog-covers.js` 已经是 1600×900 —— 把那张 `hearth-frame-blog-in-situ.png`（2000×2000）加入生成列表，重跑，确认输出尺寸为 1600×900。

- [ ] **Step 4: 重启 PM2 并验证**

`public/` 变了必须重启（Next 启动时快照 public 目录，否则新图经 `/_next/image` 会 400）。

```bash
ssh kyno 'cd /opt/kyno && pm2 restart kyno'
```
```bash
curl -s "https://www.kynocreative.com/_next/image?url=%2Fuploads%2Fhearth-frame-blog-in-situ.png&w=1080&q=75" -o /dev/null -w "%{http_code}\n"  # 期望 200
```

> **本步骤不要验证「分类标签渲染出来了」。** 此时线上跑的还是旧代码，不认识
> `category` 字段 —— 标签要等 Task 10 部署新代码后才会出现。这里只确认
> **图片经 `/_next/image` 返回 200**（证明 public 目录重启生效）以及
> `data/blog-posts.json` 里 7 篇都写入了预期的分类。

- [ ] **Step 5: Commit（仅代码侧改动）**

```bash
git add scripts/make-blog-covers.js
git commit -m "chore(blog): regenerate the square cover at 16:9"
```

---

### Task 10: 部署与端到端验证

**Files:** 无（只做发布与验证）

- [ ] **Step 1: 打包上传改动文件**

按本项目既有做法（**不是 git pull**）：本地 `tar` 打包 → `scp` 到 `/tmp` → 服务器解压到 `/opt/kyno`。

- [ ] **Step 2: 构建**

`pm2 stop kyno` → `nohup npm run build > build.log 2>&1 &` → **轮询确认 `next build` 进程退出**（`ps -eo args | grep -c "next build"`），并用 `tail build.log` 确认打出 `✓ Generating static pages`。

> ⚠️ **不要用 `cat .next/BUILD_ID` 判断构建完成** —— 它在构建早期就写入了。构建中途重启 PM2 会损坏 `.next` 导致宕机。

- [ ] **Step 3: 重启并验证**

`pm2 restart kyno`，然后逐项 `curl` 线上：

```bash
for p in / /blog /blog/<slug> /products; do curl -s -o /dev/null -w "$p %{http_code}\n" https://www.kynocreative.com$p; done
```

- [ ] **Step 4: 浏览器验收（用户执行）**

本次改动全是视觉与交互。按项目惯例由用户在浏览器过一遍：录入一篇草稿看工具栏/预览/分类/摘要自动生成，再看详情页 hero 与文末四块、列表页置顶大卡与筛选 tab、以及**在系统里开启「减少动态效果」后进度条与动画是否静止**。

---

## Self-Review

**Spec coverage：**
| Spec 要求 | 任务 |
|---|---|
| `category` 字段 + `BLOG_CATEGORIES` | Task 1 |
| 工具栏 / 实时预览 / 正文插图 | Task 3、4 |
| 摘要自动生成、标题计数、阅读时长、分类下拉 | Task 2、5 |
| 详情页 hero + 进度条 + 文末四块 | Task 7 |
| `BlogPosting` JSON-LD | Task 7 Step 4 |
| 列表页置顶大卡 + 竖版网格 + 分类筛选（无新 URL） | Task 8 |
| `markdown.css` 补齐（含表格滚动） | Task 6 |
| 无障碍（aria-label、tabindex、对比度） | Task 4（aria-label）、Task 6（tabindex）、Task 7（hero 渐变） |
| 内容回填 + 封面重出 | Task 9 |
| 部署 | Task 10 |

**无 placeholder**：所有步骤都给了可执行内容或确切命令，无 TBD。

**类型一致性**：`BLOG_CATEGORIES`（Task 1）→ 被 Task 5、8 消费；`applyFormat`/`FormatAction`（Task 3）→ 被 Task 4 消费；`deriveExcerpt` / `readingMinutes` / `filterByCategory` / `selectRelatedPosts` / `selectRelatedProducts`（Task 2）→ 被 Task 5、7、8 消费；`MarkdownEditor` 的 `uploadUrl` 参数（Task 4）→ 被 Task 5 以两种端点值调用。

**已实测确认的工具链事实**（预检探针，不再是「不确定项」）：
- `node --test` 能直接 import `.ts`（Node 24 类型剥离），**但必须带扩展名** `"./x.ts"`；不带扩展名报 `ERR_MODULE_NOT_FOUND`。
- `node --test` **解析不了 `@/` 别名**。因此 `blog-content.ts` 必须零运行时依赖（见 Task 2）。
- `src/components/*.tsx` 里的 `@/` 别名不受影响 —— 那是 Next/TS 解析的，只在测试里失效。

**跨任务的文件/接口交集**：
| 交集 | 风险 | 处置 |
|---|---|---|
| Task 6 与 Task 7 都改 `blog/[slug]/page.tsx` | Task 7 重写 hero 时可能丢掉 Task 6 加的 `components` prop | Task 7 的 Interfaces 已写明「只替换 h1 + 封面那一段，保留 prop」 |
| Task 7 用 `BlogCard`，Task 8 把 `BlogCard` 改成竖版 | 横排会在 Task 8 之后被挤变形 | Task 7 已指定用 `sm:grid-cols-3` 网格，改造前后都成立 |
| Task 1 的输出类型被 Task 5/8 消费 | 漏传 `category` | 已把 input 类型声明为必填但可为 undefined，编译期兜住 |
| Task 2 的输出被 Task 4/5/7/8 消费 | 函数名不一致 | Interfaces 块列出了确切签名 |
| Task 9 与 Task 10 的顺序 | Task 9 验证「标签渲染」会失败 | Task 9 Step 4 已注明该验证推迟到 Task 10 |
